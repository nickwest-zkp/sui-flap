SuiFlap is a Sui-native launchpad prototype for DeepBook-ready `Coin<T>` launches, creator vaults, and PvP token experiments. The main production path uses static Move coin packages that create shared launch curves and can graduate liquidity into DeepBook, while the older object-token path remains available for app testing and duel mechanics.
